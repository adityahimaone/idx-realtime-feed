"""
IDX Recommendation Bot — CLI dispatcher

Usage:
    python -m bot.pre_ara                    # single run
    python -c "from bot.morning import run_and_send; import asyncio; asyncio.run(run_and_send('early'))"
"""
